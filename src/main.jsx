import React from 'react';
import ReactDOM from 'react-dom/client';
import DAToolkit from './DAToolkit.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DAToolkit />
  </React.StrictMode>
);
